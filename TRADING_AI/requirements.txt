# TRADING_AI - all free and open-source.
# Pinned to major versions to stay reproducible without freezing bug fixes.

# Core data handling
pandas>=2.0
numpy>=1.24
requests>=2.31
PyYAML>=6.0

# Dashboard (local only, no cloud)
streamlit>=1.30
plotly>=5.18

# ML - only needed from step 13 onward
scikit-learn>=1.3

# Testing
pytest>=7.4
