<?xml version="1.0" encoding="utf-8"?>
<!--
  Phase 3 placeholder. Deliberately minimal: it does NOT perform gestures yet.
  It only reports which INVO screens are visible, which lets us prove that
  INVO exposes readable UI nodes before any automation is written.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_label"
    android:notificationTimeout="200" />
